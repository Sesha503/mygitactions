name: Master

on:
  push:
    branches:
      - master
  schedule:
    - cron: '30 17 * * *'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - name: Running
        run: |
          echo "Running!"

  test:
    runs-on: ubuntu-latest
    steps:
      - name: 23f2003661@ds.study.iitm.ac.in
        run: echo "Hello, world!"
